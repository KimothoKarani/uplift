services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: uplift
      POSTGRES_PASSWORD: uplift
      POSTGRES_DB: uplift
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data: